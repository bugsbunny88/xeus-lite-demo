name: xeus-kernel
channels:
  - https://repo.prefix.dev/emscripten-forge-dev
  - https://repo.prefix.dev/conda-forge
dependencies:
  - xeus-python

    # Scientific computing & data analysis
  - numpy>=1,<2           # Expanded upper bound: Pyodide is 2.2.5, original was <2
  - pandas>=1.5.3,<3      # Expanded upper bound: Pyodide is 2.3.2, original was <2
  - matplotlib>=3.5,<4    # Pyodide (3.8.4) fits within original range
  - scipy>=1.11.1,<2      # Pyodide (1.14.1) fits within original range
  - scikit-learn>=1.1.1,<2 # Pyodide (1.7.0) fits within original range
  - altair>=5.5.0,<7      # Widened lower bound: Pyodide is 5.5.0, original was >=6.0.0
  - seaborn
  - great_tables

  # Data processing & utilities
  - requests>=2.32.4,<3   # Widened lower bound: Pyodide is 2.32.4, original was >=2.32.5
  - toolz>=1.0.0,<2       # Widened lower bound: Pyodide is 1.0.0, original was >=1.1.0

  # Schema & validation
  - jsonschema>=4.17.3,<5 # Pyodide (4.23.0) fits within original range
  - pydantic>=2.8.2,<3    # Pyodide (2.10.6) fits within original range

  # CLI & UX
  - rich>=13.9.4,<15      # Widened lower bound: Pyodide is 13.9.4, original was >=14.2.0

  # Data formats & serialization
  - orjson>=3.10.6,<4     # Pyodide (3.10.16) fits within original range
  - pytz>=2025.2,<2026    # Pyodide (2025.2) fits within original range
  - beautifulsoup4>=4.13.3,<5 # Widened lower bound: Pyodide is 4.13.3, original was >=4.14.2

  # SQL & query
  - jinja2>=3.1.6,<4      # Pyodide (3.1.6) fits within original range

  # Storage / arrays / misc
  - brotli>=1.1.0,<2      # Pyodide (1.1.0) fits within original range
  - cffi>=1.15.1,<2       # Pyodide (1.17.1) fits within original range
  - cryptography>=42.0.2,<47 # Expanded upper bound: Pyodide is 46.0.1, original was <43
  - docutils>=0.18.1,<0.22 # Expanded upper bound: Pyodide is 0.21.2, original was <0.19
  - fonttools>=4.39.4,<5  # Pyodide (4.56.0) fits within original range
  - future>=1.0.0,<2      # Pyodide (1.0.0) fits within original range
  - joblib>=1.4.0,<2      # Pyodide (1.4.2) fits within original range
  - kiwisolver>=1.3.2,<2  # Pyodide (1.4.8) fits within original range
  - logbook>=1.6.0,<2     # Pyodide (1.8.0) fits within original range
  - markupsafe>=3.0.2,<4  # Pyodide (3.0.2) fits within original range
  - patsy>=1.0.1,<2       # Pyodide (1.0.1) fits within original range
  - peewee>=3.17.3,<4     # Pyodide (3.17.9) fits within original range
  - pyrsistent>=0.18.1,<0.21 # Expanded upper bound: Pyodide is 0.20.0, original was <0.19
  - pytest>=8.3.2,<9      # Pyodide (8.3.5) fits within original range
  - pywavelets>=1.4.1,<2  # Pyodide (1.8.0) fits within original range
  - regex>=2022.1.18,<2025 # Expanded upper bound: Pyodide is 2024.11.6, original was <2023
  - sympy>=1.13.3,<2      # Widened lower bound: Pyodide is 1.13.3, original was >=1.14.0
  - traits>=7.0.2,<8      # Pyodide (7.0.2) fits within original range
  - wrapt>=1.17.2,<2      # Pyodide (1.17.2) fits within original range
  - diskcache>=5.6.3,<6   # Pyodide (5.6.3) fits within original range
  - aiohttp>=3.11.13,<4   # Widened lower bound: Pyodide is 3.11.13, original was >=3.13.2
